# API Errors Problem JSON Rule

Opt-in rule for **RFC 9457** error contracts while editing APIs.

## When to use

Error handlers, OpenAPI error schemas, validation responses.

## Usage

Enable the `.mdc` in Cursor after copying/linking this pack into project rules.
