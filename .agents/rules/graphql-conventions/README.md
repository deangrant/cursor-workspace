# GraphQL Conventions Rule

Opt-in GraphQL schema and auth conventions while editing GraphQL code.

## When to use

Schema PRs, resolvers, GraphQL server wiring.

## Usage

Enable the `.mdc` in Cursor when working on GraphQL.
