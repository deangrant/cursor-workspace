# API Security Baseline Rule

Opt-in OWASP-aligned security constraints for HTTP API work.

## When to use

Security reviews, new endpoints, authz changes.

## Usage

Enable the `.mdc` rule pack in Cursor when editing API code.
