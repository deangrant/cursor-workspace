# GraphQL Security Baseline Rule

Opt-in thin GraphQL hardening constraints. Pairs with `graphql-security` skill.

## When to use

GraphQL server/resolver edits, production lock-down.

## Usage

Enable alongside `graphql-conventions` when editing GraphQL.
