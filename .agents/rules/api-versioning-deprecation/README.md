# API Versioning Deprecation Rule

Opt-in rule for version bumps and sunset signaling.

## When to use

Breaking changes, retiring fields/versions, migration headers.

## Usage

Enable the `.mdc` when editing versioned APIs or deprecation tooling.
