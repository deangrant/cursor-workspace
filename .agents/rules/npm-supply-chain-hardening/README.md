# npm Supply-Chain Hardening Rule

Opt-in dependency hygiene when editing package manifests and lockfiles.

## When to use

Adding/upgrading npm packages, lockfile PRs, supply-chain reviews.

## Usage

Enable the `.mdc` (globs target package/lock files).
