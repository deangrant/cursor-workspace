# API Pagination Query Conventions Rule

Opt-in rule for list endpoint filtering, sorting, and pagination consistency.

## When to use

Collection handlers, query param design, OpenAPI list operations.

## Usage

Enable the `.mdc` when editing list/search endpoints.
