# GraphQL Schema Design — Reference

## Anti-patterns

- Exporting SQL tables as GraphQL types
- `/v2` schema instead of additive evolution
- Defaulting everything to non-null
- Auth logic only in GraphQL resolvers (REST bypass)

## Sources

- GraphQL best practices: thinking in graphs, schema design, global object ID
