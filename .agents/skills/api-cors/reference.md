# CORS — Reference

## Common headers

`Access-Control-Allow-Origin`, `Allow-Credentials`, `Allow-Methods`,
`Allow-Headers`, `Expose-Headers`, `Max-Age`.

## Anti-patterns

- `Allow-Origin: *` with credentials
- Reflecting arbitrary Origin
- Treating CORS as authentication

## Sources

- Fetch CORS
