# HATEOAS — Reference

## Why rare

Client tooling gaps, payload overhead, SDKs assume static endpoints, low ROI
for single first-party clients.

## Anti-patterns

- Hard-coded URLs while claiming HATEOAS
- Returning all possible actions regardless of state
- Confusing OpenAPI links with runtime hypermedia

## Sources

- HAL; JSON:API
