# GraphQL Performance — Reference

## Anti-patterns

- Uncached DB round trip per list item
- No max on connection `first`
- Assuming POST bodies are CDN-cacheable without persisted GET

## Sources

- GraphQL performance best practices
