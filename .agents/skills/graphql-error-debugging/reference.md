# GraphQL Error Debugging — Reference

## Media types

- Modern: `application/graphql-response+json` — validation may be 4xx
- Legacy: `application/json` — often 200 + errors even without data

## Sources

- GraphQL common errors + serving over HTTP best practices
