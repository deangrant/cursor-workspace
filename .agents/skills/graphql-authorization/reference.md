# GraphQL Authorization — Reference

## Anti-patterns

- Auth only in GraphQL resolvers
- Rejecting entire request for one forbidden field when partial is required
- Passing opaque tokens into domain rules
- Directive-only auth duplicated from REST

## Sources

- GraphQL authorization best practices
